spring:
  jpa:
    open-in-view: false
    properties:
      hibernate:
        transaction:
          jta:
            platform: false
  # 数据库连接信息
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      # 连接池名称
      pool-name: HikariCP-MySQL
      # 最小空闲连接数
      minimum-idle: 2
      # 最大连接数
      maximum-pool-size: 20
      # 连接超时时间（毫秒）
      connection-timeout: 60000
      # 空闲连接存活时间（毫秒）
      idle-timeout: 300000
      # 连接最大存活时间（毫秒）
      max-lifetime: 1800000
      # 自动提交
      auto-commit: true
      # 连接测试查询
      connection-test-query: SELECT 1
      # 验证连接的有效性
      validation-timeout: 5000
      # 连接泄漏检测阈值（毫秒）
      leak-detection-threshold: 60000
      # MySQL特定配置
      data-source-properties:
        # 预处理语句缓存
        cachePrepStmts: true
        prepStmtCacheSize: 250
        prepStmtCacheSqlLimit: 2048
        # 性能优化
        useServerPrepStmts: true
        useLocalSessionState: true
        useLocalTransactionState: true
        rewriteBatchedStatements: true
        cacheResultSetMetadata: true
        cacheServerConfiguration: true
        elideSetAutoCommits: true
        maintainTimeStats: false
  sql:
    init:
      platform: mysql
      schema-locations: classpath:db/mysql/tables_xxl_job.sql
      encoding: utf-8
logging:
  level:
    com.zaxxer.hikari: WARN

---
spring:
  config:
    activate:
      on-profile: dev
  # 数据库连接信息
  datasource:
    url: jdbc:mysql://data.luopc.com:3306/xxl_job_dev?allowPublicKeyRetrieval=true&characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dev_test
    password: dev@123

---
spring:
  config:
    activate:
      on-profile: uat
  # 数据库连接信息
  datasource:
    url: jdbc:mysql://data.luopc.com:3306/xxl_job_uat?allowPublicKeyRetrieval=true&characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: uat_test
    password: uat@123
